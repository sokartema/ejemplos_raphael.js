.row.triple-pad-top
    .one.half
        %code.block{:style => "height:300px; max-height:300px;overflow-y:scroll;"}
            %pre.code
                =preserve do
                    :escaped
                        window.onload = function(){

                            var contenedor = document.getElementById('canvas_container');
                            var paper = new Raphael(contenedor,500,500)
                            circle = paper.circle(250,150, 80);
                            circle.attr({fill:'#ff0000'});


                        }

    .one.half
        #canvas_container

%script{:type => "text/javascript" , :src => "/js/raphael/ejemplo1.js"}
